const mocha = require('mocha');
const assert = require('assert');

//describe test
describe('some demo tests', funtion(){
  //create tests
  it('adds two numbers together', function() {
    assert(2 + 3 === 5);
  });
})
